import { Title } from '@angular/platform-browser';
import { Router, ActivatedRoute } from '@angular/router';
import 'rxjs/add/operator/filter';
import { MetaLoader } from './meta.loader';
export declare class MetaService {
    loader: MetaLoader;
    private readonly router;
    private readonly document;
    private readonly titleService;
    private readonly activatedRoute;
    private readonly metaSettings;
    private readonly isMetaSet;
    constructor(loader: MetaLoader, router: Router, document: any, titleService: Title, activatedRoute: ActivatedRoute);
    setTitle(title: string, override?: boolean): void;
    setTag(tag: string, value: string): void;
    private createMetaTag(name);
    private getOrCreateMetaTag(name);
    private updateLocales(currentLocale, availableLocales);
    private updateMeta(currentUrl, meta?);
}
